# TODO

- providers: azure, groq, cerebras. they have models too. how do we add them?
- would be nice to build time evaluate `defineModelCapabilities` and inline the result into the dist js. comptime tsdown plugin or something in js?
